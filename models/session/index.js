import { AddSession, GetSession } from './factory'
import { Get, Add, AddEntities } from './rest'

export let Factory = {
  AddSession,
  GetSession
}

export let Rest = {
  Get,
  Add,
  AddEntities
}
